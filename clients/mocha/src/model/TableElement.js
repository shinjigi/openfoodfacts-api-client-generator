/**
 * Open Food Facts Open API
 * As a developer, the Open Food Facts API allows you to get information and contribute to the products database. You can create great apps to help people make better food choices and also provide data to enhance the database. 
 *
 * The version of the OpenAPI document: 2
 * Contact: contact@openfoodfacts.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TableColumn from './TableColumn';

/**
 * The TableElement model module.
 * @module model/TableElement
 * @version 2
 */
class TableElement {
    /**
     * Constructs a new <code>TableElement</code>.
     * Element to display a table.
     * @alias module:model/TableElement
     */
    constructor() { 
        
        TableElement.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TableElement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TableElement} obj Optional instance to populate.
     * @return {module:model/TableElement} The populated <code>TableElement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TableElement();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], 'String');
            }
            if (data.hasOwnProperty('columns')) {
                obj['columns'] = ApiClient.convertToType(data['columns'], [TableColumn]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TableElement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TableElement</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['rows'] && !(typeof data['rows'] === 'string' || data['rows'] instanceof String)) {
            throw new Error("Expected the field `rows` to be a primitive type in the JSON string but got " + data['rows']);
        }
        if (data['columns']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['columns'])) {
                throw new Error("Expected the field `columns` to be an array in the JSON data but got " + data['columns']);
            }
            // validate the optional field `columns` (array)
            for (const item of data['columns']) {
                TableColumn.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * An id for the table.
 * @member {String} id
 */
TableElement.prototype['id'] = undefined;

/**
 * Title of the column. 
 * @member {String} title
 */
TableElement.prototype['title'] = undefined;

/**
 * @member {String} rows
 */
TableElement.prototype['rows'] = undefined;

/**
 * @member {Array.<module:model/TableColumn>} columns
 */
TableElement.prototype['columns'] = undefined;






export default TableElement;

