/**
 * Open Food Facts Open API
 * As a developer, the Open Food Facts API allows you to get information and contribute to the products database. You can create great apps to help people make better food choices and also provide data to enhance the database. 
 *
 * The version of the OpenAPI document: 2
 * Contact: contact@openfoodfacts.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Nutrient model module.
 * @module model/Nutrient
 * @version 2
 */
class Nutrient {
    /**
     * Constructs a new <code>Nutrient</code>.
     * @alias module:model/Nutrient
     */
    constructor() { 
        
        Nutrient.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Nutrient</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Nutrient} obj Optional instance to populate.
     * @return {module:model/Nutrient} The populated <code>Nutrient</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Nutrient();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('important')) {
                obj['important'] = ApiClient.convertToType(data['important'], 'Boolean');
            }
            if (data.hasOwnProperty('display_in_edit_form')) {
                obj['display_in_edit_form'] = ApiClient.convertToType(data['display_in_edit_form'], 'Boolean');
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = ApiClient.convertToType(data['unit'], 'String');
            }
            if (data.hasOwnProperty('nutrients')) {
                obj['nutrients'] = ApiClient.convertToType(data['nutrients'], [Nutrient]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Nutrient</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Nutrient</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['unit'] && !(typeof data['unit'] === 'string' || data['unit'] instanceof String)) {
            throw new Error("Expected the field `unit` to be a primitive type in the JSON string but got " + data['unit']);
        }
        if (data['nutrients']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['nutrients'])) {
                throw new Error("Expected the field `nutrients` to be an array in the JSON data but got " + data['nutrients']);
            }
            // validate the optional field `nutrients` (array)
            for (const item of data['nutrients']) {
                Nutrient.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * id of the nutrient
 * @member {String} id
 */
Nutrient.prototype['id'] = undefined;

/**
 * Name of the nutrient in the requested language
 * @member {String} name
 */
Nutrient.prototype['name'] = undefined;

/**
 * Indicates if the nutrient is always shown on the nutrition facts table
 * @member {Boolean} important
 */
Nutrient.prototype['important'] = undefined;

/**
 * Indicates if the nutrient should be shown in the nutrition facts edit form
 * @member {Boolean} display_in_edit_form
 */
Nutrient.prototype['display_in_edit_form'] = undefined;

/**
 * Default unit of the nutrient
 * @member {module:model/Nutrient.UnitEnum} unit
 */
Nutrient.prototype['unit'] = undefined;

/**
 * @member {Array.<module:model/Nutrient>} nutrients
 */
Nutrient.prototype['nutrients'] = undefined;





/**
 * Allowed values for the <code>unit</code> property.
 * @enum {String}
 * @readonly
 */
Nutrient['UnitEnum'] = {

    /**
     * value: "g"
     * @const
     */
    "g": "g",

    /**
     * value: "mg"
     * @const
     */
    "mg": "mg",

    /**
     * value: "μg"
     * @const
     */
    "μg": "μg",

    /**
     * value: "cl"
     * @const
     */
    "cl": "cl",

    /**
     * value: "ml"
     * @const
     */
    "ml": "ml",

    /**
     * value: "dv"
     * @const
     */
    "dv": "dv",

    /**
     * value: "% vol"
     * @const
     */
    "% vol": "% vol",

    /**
     * value: "%"
     * @const
     */
    "PERCENT": "%"
};



export default Nutrient;

