/**
 * Open Food Facts Open API
 * As a developer, the Open Food Facts API allows you to get information and contribute to the products database. You can create great apps to help people make better food choices and also provide data to enhance the database. 
 *
 * The version of the OpenAPI document: 2
 * Contact: reuse@openfoodfacts.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import AttributeGroup from '../model/AttributeGroup';
import GetPreferences200ResponseInner from '../model/GetPreferences200ResponseInner';
import GetProductByBarcodeAllResponse from '../model/GetProductByBarcodeAllResponse';
import Nutrient from '../model/Nutrient';
import OcrOnProductResponse from '../model/OcrOnProductResponse';
import SearchForProductsResponse from '../model/SearchForProductsResponse';

/**
* ReadRequests service.
* @module api/ReadRequestsApi
* @version 2
*/
export default class ReadRequestsApi {

    /**
    * Constructs a new ReadRequestsApi. 
    * @alias module:api/ReadRequestsApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }


    /**
     * Callback function to receive the result of the getAttributeGroups operation.
     * @callback module:api/ReadRequestsApi~getAttributeGroupsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/AttributeGroup>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get the list of attributes available for personal search.
     * Attributes are at the heart of personal search. They score the products according to different criterias, which could then be matched to a user's preferences.  This API helps you list attributes and display them in your application, for the user to choose the importance of each criteria.  note: /api/v2/attribute_groups_{lc} is also a valid route, but consider it deprecated 
     * @param {Object} opts Optional parameters
     * @param {String} [lc] 2 letter code of the language of the user. Used for localizing some fields in returned values (e.g. knowledge panels). If not passed, the language may be inferred by the Accept-Language header of the request, or from the domain name prefix. 
     * @param {module:api/ReadRequestsApi~getAttributeGroupsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/AttributeGroup>}
     */
    getAttributeGroups(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'lc': opts['lc']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [AttributeGroup];
      return this.apiClient.callApi(
        '/api/v2/attribute_groups', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getCgiIngredientsPl operation.
     * @callback module:api/ReadRequestsApi~getCgiIngredientsPlCallback
     * @param {String} error Error message, if any.
     * @param {module:model/OcrOnProductResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Performing OCR on a Product
     * Open Food Facts uses optical character recognition (OCR) to retrieve nutritional data and other information from the product labels. 
     * @param {String} id 
     * @param {String} code Barcode of the product
     * @param {String} process_image 
     * @param {String} ocr_engine 
     * @param {module:api/ReadRequestsApi~getCgiIngredientsPlCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/OcrOnProductResponse}
     */
    getCgiIngredientsPl(id, code, process_image, ocr_engine, callback) {
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getCgiIngredientsPl");
      }
      // verify the required parameter 'code' is set
      if (code === undefined || code === null) {
        throw new Error("Missing the required parameter 'code' when calling getCgiIngredientsPl");
      }
      // verify the required parameter 'process_image' is set
      if (process_image === undefined || process_image === null) {
        throw new Error("Missing the required parameter 'process_image' when calling getCgiIngredientsPl");
      }
      // verify the required parameter 'ocr_engine' is set
      if (ocr_engine === undefined || ocr_engine === null) {
        throw new Error("Missing the required parameter 'ocr_engine' when calling getCgiIngredientsPl");
      }

      let pathParams = {
      };
      let queryParams = {
        'id': id,
        'code': code,
        'process_image': process_image,
        'ocr_engine': ocr_engine
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = OcrOnProductResponse;
      return this.apiClient.callApi(
        '/cgi/ingredients.pl', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getCgiNutrientsPl operation.
     * @callback module:api/ReadRequestsApi~getCgiNutrientsPlCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/Nutrient>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get a nested list of nutrients that can be displayed in the nutrition facts table for a specific country and language
     * Used to display the nutrition facts table of a product, or to display a form to input those nutrition facts. 
     * @param {Object} opts Optional parameters
     * @param {String} [cc] 2 letter code of the country of the user. Used for localizing some fields in returned values (e.g. knowledge panels). If not passed, the country may be inferred by the IP address of the request.
     * @param {String} [lc] 2 letter code of the language of the user. Used for localizing some fields in returned values (e.g. knowledge panels). If not passed, the language may be inferred by the Accept-Language header of the request, or from the domain name prefix. 
     * @param {module:api/ReadRequestsApi~getCgiNutrientsPlCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/Nutrient>}
     */
    getCgiNutrientsPl(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'cc': opts['cc'],
        'lc': opts['lc']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [Nutrient];
      return this.apiClient.callApi(
        '/cgi/nutrients.pl', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getCgiSuggestPl operation.
     * @callback module:api/ReadRequestsApi~getCgiSuggestPlCallback
     * @param {String} error Error message, if any.
     * @param {Array.<Object>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Suggestions to Aid Adding/Editing Products
     * For example , Dave is looking for packaging_shapes that contain the term \"fe\", all packaging_shapes containing \"fe\" will be returned. This is useful if you have a search in your application, for a specific product field. 
     * @param {Object} opts Optional parameters
     * @param {String} [tagtype] 
     * @param {String} [term] 
     * @param {module:api/ReadRequestsApi~getCgiSuggestPlCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<Object>}
     */
    getCgiSuggestPl(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'tagtype': opts['tagtype'],
        'term': opts['term']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [Object];
      return this.apiClient.callApi(
        '/cgi/suggest.pl', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getPreferences operation.
     * @callback module:api/ReadRequestsApi~getPreferencesCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/GetPreferences200ResponseInner>} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get the weights corresponding to attributes preferences to compute personal product 
     * This endpoint retrieves the weights corresponding to attribute preferences for computing personal product recommendations. The weights are used to personalize the product recommendations based on user preferences. 
     * @param {Object} opts Optional parameters
     * @param {String} [lc] 2 letter code of the language of the user. Used for localizing some fields in returned values (e.g. knowledge panels). If not passed, the language may be inferred by the Accept-Language header of the request, or from the domain name prefix. 
     * @param {module:api/ReadRequestsApi~getPreferencesCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link Array.<module:model/GetPreferences200ResponseInner>}
     */
    getPreferences(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'lc': opts['lc']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [GetPreferences200ResponseInner];
      return this.apiClient.callApi(
        '/api/v2/preferences', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getProductByBarcode operation.
     * @callback module:api/ReadRequestsApi~getProductByBarcodeCallback
     * @param {String} error Error message, if any.
     * @param {module:model/GetProductByBarcodeAllResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get information for a specific product by barcode
     * Fetches product details by its unique barcode.  Can return all product details or specific fields like knowledge panels. 
     * @param {String} barcode The barcode of the product to be fetched
     * @param {Object} opts Optional parameters
     * @param {String} [fields] Specific fields to return. Use 'knowledge_panels' for Knowledge Panels only.
     * @param {module:api/ReadRequestsApi~getProductByBarcodeCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/GetProductByBarcodeAllResponse}
     */
    getProductByBarcode(barcode, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'barcode' is set
      if (barcode === undefined || barcode === null) {
        throw new Error("Missing the required parameter 'barcode' when calling getProductByBarcode");
      }

      let pathParams = {
        'barcode': barcode
      };
      let queryParams = {
        'fields': opts['fields']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = GetProductByBarcodeAllResponse;
      return this.apiClient.callApi(
        '/api/v2/product/{barcode}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }

    /**
     * Callback function to receive the result of the getSearch operation.
     * @callback module:api/ReadRequestsApi~getSearchCallback
     * @param {String} error Error message, if any.
     * @param {module:model/SearchForProductsResponse} data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Search for Products
     * Search request allows you to get products that match your search criteria.  It allows you create many custom APIs for your use case.  If the search query parameter has 2 possible values, they are seperated by a comma(,). When filtering via a parameter that has different language codes like `fr`, `de` or `en`, specify the language code in the parameter name e.g `categories_tags_en`  **Important:** search API v2 does not support full text request (search_term), you have to use [search API v1](https://wiki.openfoodfacts.org/API/Read/Search) for that. Upcoming [search-a-licious project](https://github.com/openfoodfacts/search-a-licious) will fix that.  ### Limiting results  You can limit the size of returned objects thanks to the `fields` object (see below).  eg: `fields=code,product_name,brands,attribute_groups``  Please use it as much as possible to avoid overloading the servers.  The search use pagination, see `page` and `page_size` parameters.  **Beware:** the `page_count` data in item is a bit counter intuitive…, read the description.  ### Conditions on tags  All `_tags`` parameters accepts either:  * a single value * or a comma-separated list of values (doing a AND) * or a pipe separated list of values (doing a OR)  You can exclude terms by using a \"-\" prefix.  For taxonomized entries, you might either use the tag id (recommended), or a known synonym (without language prefix)  * `labels_tags=en:organic,en:fair-trade` find items that are fair-trade AND organic * `labels_tags=en:organic|en:fair-trade` find items that are fair-trade OR organic * `labels_tags=en:organic,en:-fair-trade` find items that are organic BUT NOT fair-trade   ### Conditions on nutriments  To get a list of nutrients  You can either query on nutrient per 100g (`_100g` suffix) or per serving (`serving` suffix).  You can also add `_prepared_` to get the nutrients in the prepared product instead of as sold.  You can add a comparison operator and value to the parameter name to get products with nutrient above or bellow a value. If you use a parameter value it exactly match it.  * `energy-kj_100g<200` products where energy in kj for 100g is less than 200kj * `sugars_serving>10` products where sugar per serving is greater than 10g * `saturated-fat_100g=1` products where saturated fat per 100g is exactly 10g * `salt_prepared_serving<0.1` products where salt per serving for prepared product is less than 0.1g  ### More references  See also [wiki page](https://wiki.openfoodfacts.org/Open_Food_Facts_Search_API_Version_2) 
     * @param {Object} opts Optional parameters
     * @param {String} [additives_tags] The additives_tags in english of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/additives) has a list of possible values for `additives`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [allergens_tags] The allergens_tags in english of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/allergens) has a list of possible values for `allergens`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [brands_tags] The brands_tags of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/brands) has a list of possible values for `brands`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [categories_tags] The category of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/categories) has a list of possible values for `categories`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [countries_tags_en] The countries_tags_en of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/countries) shows a list of possible values for `countries`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [emb_codes_tags] The emb_codes_tags of product(s) you are searching for.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [labels_tags] The labels_tags in english of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/labels) has a list of possible values for `labels`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [manufacturing_places_tags] The manufacturing_places_tags of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/manufacturing-places) has a list of possible values for `manufacturing-places`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [nutrition_grades_tags] The nutrition_grades_tags of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/nutrition-grades) has a list of possible values for `nutrition-grades`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [origins_tags] The origins_tags of product(s) you are searching for.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [packaging_tags_de] The packaging_tag in german of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/packaging) has a list of possible values for `packaging`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [purchase_places_tags] The purchase_places_tags of product(s) you are searching for.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [states_tags] The states_tags in english of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/states) has a list of possible values for `states`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [stores_tags] The stores_tags of product(s) you are searching for.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {String} [traces_tags] The traces_tags of product(s) you are searching for. The [OFF App](https://world.openfoodfacts.org/traces) shows a list of possible values for `traces`.  You can use multiple values by using a comma separated list. You can add a \"-\" before values to avoid matching a tag. 
     * @param {Object.<String, Object>} [tag_name_tags_language_code] You can add a language code to a specific tag to query it in a specific language 
     * @param {Object.<String, Object>} [nutrient_lt_value] Search on nutrient lower than a value 
     * @param {Object.<String, Object>} [nutrient_gt_value] Search on nutrient greater than a value 
     * @param {Object.<String, Object>} [nutrient_eq_value] Search on nutrient for an exact quantity 
     * @param {String} [fields] Specific fields to return. Use 'knowledge_panels' for Knowledge Panels only.
     * @param {module:model/String} [sort_by] The allowed values  used to sort/order the search results.  * `product_name` sorts on name * `ecoscore_score`, `nova_score`, `nutriscore_score` rank on the [Eco-Score](https://world.openfoodfacts.org/eco-score-the-environmental-impact-of-food-products), [Nova](https://world.openfoodfacts.org/nova), or [Nutri-Score](https://world.openfoodfacts.org/nutriscore) * `scans_n`, `unique_scans_n` and `popularity_key` are about product popularity: number of scans on unique scans, rank of product * `created_t`, `last_modified_t`, are about creation and modification dates * `nothing`, tells not to sort at all (because if you do not provide the sort_by argument we default to sorting on popularity (for food) or last modification date) 
     * @param {Number} [page] The page number you request to view (eg. in search results spanning multiple pages) 
     * @param {Number} [page_size] The number of elements should be sent per page 
     * @param {module:api/ReadRequestsApi~getSearchCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link module:model/SearchForProductsResponse}
     */
    getSearch(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'additives_tags': opts['additives_tags'],
        'allergens_tags': opts['allergens_tags'],
        'brands_tags': opts['brands_tags'],
        'categories_tags': opts['categories_tags'],
        'countries_tags_en': opts['countries_tags_en'],
        'emb_codes_tags': opts['emb_codes_tags'],
        'labels_tags': opts['labels_tags'],
        'manufacturing_places_tags': opts['manufacturing_places_tags'],
        'nutrition_grades_tags': opts['nutrition_grades_tags'],
        'origins_tags': opts['origins_tags'],
        'packaging_tags_de': opts['packaging_tags_de'],
        'purchase_places_tags': opts['purchase_places_tags'],
        'states_tags': opts['states_tags'],
        'stores_tags': opts['stores_tags'],
        'traces_tags': opts['traces_tags'],
        '&lt;tag_name&gt;_tags_&lt;language_code&gt;': opts['tag_name_tags_language_code'],
        '&lt;nutrient&gt;_lt_&lt;value&gt;': opts['nutrient_lt_value'],
        '&lt;nutrient&gt;_gt_&lt;value&gt;': opts['nutrient_gt_value'],
        '&lt;nutrient&gt;_eq_&lt;value&gt;': opts['nutrient_eq_value'],
        'fields': opts['fields'],
        'sort_by': opts['sort_by'],
        'page': opts['page'],
        'page_size': opts['page_size']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = [];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = SearchForProductsResponse;
      return this.apiClient.callApi(
        '/api/v2/search', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null, callback
      );
    }


}
